============================================================
RELATÓRIO - LAB 2
============================================================
# QUESTÃO 1
Após a compilação e execução do programa obtive o seguinte 
resultado:
0x7ffdc2081b6c - 10
0x7ffdc2081b6d - 27
0x7ffdc2081b6e - 00
0x7ffdc2081b6f - 00
onde o primeiro número representa o endereço de memória que 
aquele byte está organizado e em seguida a parte hexadecimal 
daquele byte. O resultado contém 4 linhas, mostrando que um 
inteiro tem 4 bytes.

Após a alteração do tipo para long, obtive o mesmo resultado 
porém em 8 linhas, mostrando que um long tem 8 bytes. 

Para o short, o programa imprimiu 2 linhas mostrando que um 
short tem 2 bytes.

Para o char apenas uma linha, mostrando que um char contem 1 
byte.



# QUESTÃO 2
A função string2num converte string para inteiro, ela cria uma 
variável inteira para armazenar o resultado e utiliza um "for" 
para percorrer toda a string passada no argumento e para cada 
caractere da string ela multiplica a variável por 10, ja que 
ela quer na base 10, e soma a diferenca entre o codigo do caractere 
atual e o codigo do caractere 0, ja que assim ele vai saber exatamente 
qual número em que a string se refere. A cada vez que ele percorre a 
string ele move uma dezena do valor antigo e acrescenta a respectiva 
unidade.

Para fazer com que a função aceite qualquer base entre 2 e 10, será 
necessário fazer apenas uma alteração, na formula "a = a*10 + (*s - '0')" 
ele multiplica por 10 porque está considerando tudo na base decimal 
então basta trocar o 10 por uma variável, que pode ser chamada "base", 
dada como argumento da função. A função será a seguinte:

    int string2num (char *s, int base) {
            int a = 0;
            for (; *s; s++) {
                a = a*base + (*s - '0');
            }
            return a;
        }

A função do item anterior converte string de numeros na base dada para 
inteiros decimais, no entanto essa base tem que ser algo entre 2 e 10. 
Para que ela aceite outras bases seria necessário fazer com que ela 
verifique se é digito e adicionar caso seja e adicionar um "offset", 
já que no caso dela for "a"(a primeiro letra), estará correspondendo 
ao numero 10. A função ficará assim:

    int is_digit(char c) {
        return (((c >= 'a') && (c <= 'z')) ? 1 : 0);
    }

    int string2num (char *s, int base) {
        int a = 0;
        for (; *s; s++) {
            if (is_digit(*s))
                a = a*base + (*s - 'a' + 10);
            else
                a = a*base + (*s - '0');
        }
        return a;
    }

============================================================
EXERCICIOS EXTRA - LAB 2
============================================================

# QUESTÃO 1

- A questão pede para criar a função is_little, que verifica 
se o tipo de organização de memória da minha máquina é 
big-endian ou little_endian e retorne 1 se for little-endian 
e 0 caso contrario. Para isso, criei uma variavel int contendo 
o número 1 e um ponteiro sem sinal para char que aponte para o 
byte mais significativo dessa variável, se for 1 é porque está 
organizado "01 00 00 00", ao contrário, então é little-endian, 
caso contrario está organizado "00 00 00 01", ordem "correta", 
então é big-endian e retorna 0.

    int is_little(void) {
        int c = 1;
        unsigned char *p = (unsigned char *)&c;

        return (*p == 1) ? 1 : 0;
    }

# QUESTÃO 2

- Essa questão pede para que a gente crie a função num2string 
que vai converter um numero  na base passada como argumento em 
uma string. Para isso eu criei um array contendo os possíveis 
simbolos da minha string(de 0 a 9 e de A a Z), depois tratei o 
caso da base inválida, ser menor que 2 ou maior que 16, e depois 
o caso do numero ser 0. Após isso eu usei a lógica das divisões 
sucessivas para pegar cada digito atraves dos restos e então 
fiz um "mapeamento" a partir do array que criei e armazenei 
em uma variavel temporaria para me auxiliar, já que isso iria 
guardar os digitos na ordem inversa, depois disso so inverti 
a ordem e amazenei na string dada como argumento.

    void num2string(char *s, int num, int base) {
        char *digits = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ";
        char temp[TAM_MAX];
        int i = 0;

        if (base < 2 || base > 36) {
            s[0] = '\0'; // Base inválida
            return;
        }

        if (num == 0) {
            s[0] = '0';
            s[1] = '\0';
            return;
        }

        while (num > 0 && i < TAM_MAX - 1) {
            temp[i++] = digits[num % base];
            num /= base;
        }
        temp[i] = '\0';

        // Inverter a string resultante
        for (int j = 0; j < i; j++) {
            s[j] = temp[i - j - 1];
        }
        s[i] = '\0';
    }

============================================================